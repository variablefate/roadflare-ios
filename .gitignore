# Xcode
**/xcuserdata/
*.xcodeproj/project.xcworkspace/xcshareddata/IDEWorkspaceChecks.plist
*.xcuserstate
*.xcscmblueprint
*.xccheckout
*.pbxuser
*.moved-aside
*.hmap
DerivedData/
build/
*.ipa
*.dSYM.zip
*.dSYM

# Swift Package Manager
.build/
.swiftpm/
Package.resolved
!RoadFlare/RoadFlare.xcodeproj/project.xcworkspace/xcshareddata/swiftpm/Package.resolved

# macOS
.DS_Store
.AppleDouble
.LSOverride
._*
*.swp
*.swo
*~
*.bak
*.orig

# Editors / IDEs (contributor-local)
.idea/
.vscode/
.cursor/
.zed/
.fleet/

# Contributor-local signing override (see CONTRIBUTING.md)
Local.xcconfig

# Local product docs
PRD.md

# Local-only planning, triage, and review scratch (agent-authored, contributor-
# irrelevant). Architecture decisions belong in `decisions/` (ADRs); contributor
# guidance belongs in README / RELEASING / CONTRIBUTING.
docs/superpowers/
docs/triage/
docs/plans/

# Branding backups (local-only design iterations)
Branding/backups/

# CocoaPods (not used, but just in case)
Pods/

# Fastlane
fastlane/report.xml
fastlane/Preview.html
fastlane/screenshots
fastlane/test_output
.gitnexus
# GitNexus auto-generates these with a symbol count that fluctuates between runs.
# Contributors regenerate via `npx gitnexus@1.5.3 setup`.
CLAUDE.md
AGENTS.md

# Claude Code per-session worktrees. `.claude/` itself is intentionally tracked
# (contains .claude/CLAUDE.md and .claude/skills/gitnexus/**), but the
# `.claude/worktrees/` subdirectory is local session scratch space created by
# the Claude Code CLI — never repo content.
.claude/worktrees/

# Project-local parallel worktrees (feature work, never repo content)
.worktrees/
